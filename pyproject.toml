[project]
name = "bye-bioinfo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.7,<6.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

